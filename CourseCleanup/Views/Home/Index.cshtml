@using CourseCleanup.Models
@model HomeViewModel
@{
    ViewBag.Title = "Home Page";
}

@if (true)
{
    Html.RenderPartial("_CourseCleanup", Model);
}

@*@if (Model.Authorized)
{
    Html.RenderPartial("_CourseCleanup", Model);
}
else
{
    if (ViewBag.error == null)
    {
        <div class="alert alert-info" role="alert">
            Please Sign In.
        </div>
    }
    else
    {
        <div class="alert alert-danger" role="alert">
            @ViewBag.error
        </div>
    }
}*@